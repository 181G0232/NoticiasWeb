<!DOCTYPE html>

<html lang="en" xmlns="http://www.w3.org/1999/xhtml">
<head>
    <meta charset="utf-8" />
    <title>Ejercicio de Men�s</title>  
    <link href="~/stylesWeb.css" rel="stylesheet" />
    <script src="https://kit.fontawesome.com/3d4677a3a9.js" crossorigin="anonymous"></script>
</head>
@model NoticiasWeb.Models.ViewModels.IndexViewModel

<body>

    @foreach (var cat in Model.Categorias)
    {
    <li><a href="#"> @(cat.Nombre)  </a></li>
    }

    <nav class="desplegable">

        <label for="checkdesplegable"><i class="fa fa-bars"></i></label>
        <input type="checkbox" id="checkdesplegable" />

        <div class="panel">
            <ul>
                @foreach (var cat in Model.Categorias)
                {
                <li><a href="#"> @(cat.Nombre)  </a></li>
                }
            </ul>
        </div>

    </nav>

</body>
</html>